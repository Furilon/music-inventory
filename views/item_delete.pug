extends layout 

block content 
    h1 #{title}: #{item.name}
    p= item.description 
    
    p Do you really want to delete this item?
    form(method="POST" action="")
        .form-group 
            input#itemid.form-control(type="hidden", name="itemid", value=item._id required="true")
        button(type='submit') Delete